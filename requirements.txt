# --- Ingestion ---
requests>=2.31
tenacity>=8.2
pydantic>=2.6
pandas>=2.2
pyarrow>=15.0

# --- Storage / Transformation (later phases) ---
duckdb>=0.10
dbt-core>=1.10,<1.11
dbt-duckdb>=1.10,<1.11

# --- Orchestration (later phases) ---
dagster>=1.12,<1.13
dagster-dbt>=0.28,<0.29
dagster-webserver>=1.12,<1.13

# --- Serving (FastAPI Analytical Engine) ---
fastapi>=0.110
uvicorn[standard]>=0.28

# --- Dev / test ---
pytest>=8.0
ruff>=0.3,<0.16
