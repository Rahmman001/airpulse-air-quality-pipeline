{
  "name": "airpulse",
  "version": "2.0.0",
  "private": true,
  "description": "AirPulse Global Air Quality Risk Intelligence Platform",
  "scripts": {
    "dev": "npx -y concurrently --kill-others --names \"API,UI\" --prefix-colors \"blue,green\" \"./.venv/bin/python -m uvicorn app.api.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "dev:api": "./.venv/bin/python -m uvicorn app.api.main:app --reload --port 8000",
    "dev:ui": "npm run dev --prefix frontend",
    "pipeline": "./.venv/bin/python -m warehouse.pipeline_runner",
    "pipeline:live": "./.venv/bin/python -m warehouse.pipeline_runner --live",
    "build": "npm run build --prefix frontend",
    "export:static": "./.venv/bin/python -m warehouse.export_static_data",
    "start": "npm run build && ./.venv/bin/python -m uvicorn app.api.main:app --port 8000",
    "test": "./.venv/bin/pytest tests/"
  }
}
